% !TeX encoding = UTF-8
\documentclass[variant=guia,cover=institutional,mode=screen]{ua-engineering-v6-article}
\input{../config/course-config}

\UASetClassNumber{09}
\UASetClassTitle{Arquitecturas distribuidas reales}
\UASetDocKind{Guía resuelta}

\title{Clase 09 --- Guía resuelta}
\author{\UAProfessor}
\date{\UADate}

\begin{document}

\section{Criterio de lectura}
Las resoluciones son modelos de razonamiento. Deben verificarse contra los supuestos del caso y no memorizarse como recetas independientes del dominio.

\section{Ejercicios y resoluciones completas}
\begin{uaexercise}
Defina monolito modular y explique por qué no debe considerarse automáticamente un antipatrón.
\end{uaexercise}
\begin{uaanswer}
Un monolito modular es una aplicación desplegada como unidad única, pero organizada internamente en módulos con responsabilidades diferenciadas. No es un antipatrón por definición porque puede ofrecer:
\begin{itemize}
\item transacciones locales sencillas;
\item debugging directo;
\item despliegue simple;
\item menor complejidad distribuida.
\end{itemize}

Se vuelve problemático cuando el acoplamiento interno destruye la modularidad, no por el mero hecho de ser monolítico.
\end{uaanswer}

\begin{uaexercise}
Explique qué cambia, desde el punto de vista del sistema, cuando se pasa de un monolito modular a una arquitectura de microservicios.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Compare despliegue, consistencia y debugging en monolitos y microservicios.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La comparación debe usar al menos semántica, latencia, disponibilidad, complejidad operativa y recuperación. La alternativa preferible cambia cuando cambia el costo del error en el dominio; no existe ganador universal.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Construya un ejemplo donde un sistema pequeño esté peor diseñado como microservicio que como monolito.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique qué significa arquitectura orientada a eventos y qué gana respecto de una integración puramente sincrónica.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Describa dos problemas nuevos que aparecen al pasar a un diseño event-driven.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique por qué el orden suele ser parcial y no global en arquitecturas basadas en eventos.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en monolito modular, microservicios, event-driven, bounded contexts, ownership, sagas y compensaciones. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo falla parcial entre servicios, duplicación de eventos, saga incompleta y ownership ambiguo. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Diseñe un pipeline simple de eventos para el flujo: pedido $\rightarrow$ pago $\rightarrow$ envío.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Defina saga y explique por qué aparece en microservicios.
\end{uaexercise}
\begin{uaanswer}
Una saga descompone el flujo en transacciones locales y define una compensación por cada efecto ya visible. La compensación no borra la historia: es una nueva acción de negocio, puede fallar y debe ser idempotente. La solución gana disponibilidad y pierde atomicidad instantánea.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Compare saga y transacción distribuida fuerte.
\end{uaexercise}
\begin{uaanswer}
La transacción distribuida fuerte busca atomicidad global inmediata. La saga acepta descomponer la operación en pasos locales con posibles compensaciones.

Diferencia central:
\begin{itemize}
\item transacción fuerte: más corrección fuerte, más coordinación y bloqueo;
\item saga: más disponibilidad y desacoplamiento, pero más exposición a estados intermedios y compensaciones imperfectas.
\end{itemize}
\end{uaanswer}

\begin{uaexercise}
Explique por qué una compensación no equivale a un rollback.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en monolito modular, microservicios, event-driven, bounded contexts, ownership, sagas y compensaciones. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo falla parcial entre servicios, duplicación de eventos, saga incompleta y ownership ambiguo. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Diseñe una saga para una compra online con:
\begin{itemize}
\item reserva de stock;
\item cobro;
\item despacho.
\end{itemize}
Incluya pasos de compensación.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Construya un caso donde la compensación sea costosa o imperfecta.
\end{uaexercise}
\begin{uaanswer}
Ejemplo: una reserva aérea o un envío físico ya despachado. La compensación no puede “borrar” el efecto físico como si nunca hubiera ocurrido. Solo puede emitir nuevas acciones, como devolución o cancelación, con costo adicional y posible impacto visible para el cliente.
\end{uaanswer}

\begin{uaexercise}
Explique qué es coreografía y qué es orquestación.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en monolito modular, microservicios, event-driven, bounded contexts, ownership, sagas y compensaciones. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo falla parcial entre servicios, duplicación de eventos, saga incompleta y ownership ambiguo. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Diseñe un flujo de coreografía para un alta de cliente con validación, scoring y bienvenida.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en monolito modular, microservicios, event-driven, bounded contexts, ownership, sagas y compensaciones. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo falla parcial entre servicios, duplicación de eventos, saga incompleta y ownership ambiguo. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Diseñe el mismo flujo usando orquestación.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en monolito modular, microservicios, event-driven, bounded contexts, ownership, sagas y compensaciones. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo falla parcial entre servicios, duplicación de eventos, saga incompleta y ownership ambiguo. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Compare ambos enfoques en términos de visibilidad, acoplamiento y debugging.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La comparación debe usar al menos semántica, latencia, disponibilidad, complejidad operativa y recuperación. La alternativa preferible cambia cuando cambia el costo del error en el dominio; no existe ganador universal.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique por qué la propiedad de datos es central en microservicios.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Construya un ejemplo donde dos servicios compartan la misma tabla y muestre por qué eso degrada el diseño.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique cómo cambia el problema de consistencia cuando cada servicio posee sus datos.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería ADRs, mapa de bounded contexts, flujo de saga y ownership de datos. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\end{document}
