% !TeX encoding = UTF-8
\documentclass[variant=guia,cover=institutional,mode=screen]{ua-engineering-v6-article}
\input{../config/course-config}

\UASetClassNumber{08}
\UASetClassTitle{Consistencia real: CAP, PACELC, Dynamo y Spanner}
\UASetDocKind{Guía resuelta}

\title{Clase 08 --- Guía resuelta}
\author{\UAProfessor}
\date{\UADate}

\begin{document}

\section{Criterio de lectura}
Las resoluciones son modelos de razonamiento. Deben verificarse contra los supuestos del caso y no memorizarse como recetas independientes del dominio.

\section{Ejercicios y resoluciones completas}
\begin{uaexercise}
Explique con precisión qué significa CAP y por qué la interpretación “elegir dos de tres” es incompleta o engañosa.
\end{uaexercise}
\begin{uaanswer}
CAP no es un menú estático de tres propiedades intercambiables. Es una restricción sobre sistemas replicados \emph{cuando existe partición de red}. La afirmación correcta es que, bajo partición, no es posible mantener simultáneamente consistencia fuerte observable y disponibilidad total.

La formulación “elegir dos de tres” es engañosa porque:
\begin{itemize}
\item sugiere una elección permanente e independiente del contexto;
\item oculta que la tensión relevante aparece específicamente bajo partición;
\item no distingue entre distintos niveles de consistencia.
\end{itemize}
\end{uaanswer}

\begin{uaexercise}
Defina qué significa consistencia en CAP y por qué no debe entenderse como una noción vaga de “datos correctos”.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique qué significa disponibilidad en CAP y dé un ejemplo de sistema que, ante partición, decida sacrificarla.
\end{uaexercise}
\begin{uaanswer}
Disponibilidad en CAP significa que toda solicitud a un nodo no fallado recibe respuesta. Un sistema que sacrifica disponibilidad bajo partición es uno que rechaza temporalmente operaciones si no puede verificar coherencia global. Un ejemplo típico es una base orientada a transacciones financieras fuertes.
\end{uaanswer}

\begin{uaexercise}
Construya un ejemplo concreto de partición de red y explique qué decisión debe tomar un sistema que prioriza consistencia.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique por qué PACELC es una mejora conceptual respecto de CAP.
\end{uaexercise}
\begin{uaanswer}
Porque CAP solo analiza el caso de partición, mientras que PACELC agrega la tensión entre consistencia y latencia en operación normal. Esto lo hace más realista para describir sistemas actuales, donde el costo de coordinar globalmente existe incluso sin particiones activas.
\end{uaanswer}

\begin{uaexercise}
Dé un ejemplo donde no haya partición, pero igual exista un trade-off entre consistencia y latencia.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Describa la motivación arquitectónica de Dynamo. ¿Qué tipo de aplicaciones justifican este diseño?
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

Una solución completa organiza la decisión con P-F-G-S-T: define el problema, enumera fallas, fija la garantía, presenta el protocolo y reconoce el trade-off. Debe incluir estados intermedios y qué ve el usuario si la operación no termina.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique el significado de los parámetros $N$, $R$ y $W$.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Demuestre informalmente por qué la condición $R+W>N$ garantiza intersección de quórums.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Construya un ejemplo donde, aun con $R+W>N$, un cliente pueda observar un estado atrasado.
\end{uaexercise}
\begin{uaanswer}
Supongamos que la escritura fue aceptada por el quorum, pero una de las réplicas intersectadas aún no aplicó visiblemente la última versión, o la lectura se resuelve usando una política que prioriza disponibilidad local con versiones concurrentes. La intersección aumenta la probabilidad de ver frescura, pero no elimina todos los casos de retraso o conflicto.
\end{uaanswer}

\begin{uaexercise}
Explique qué problema resuelven los vector clocks en Dynamo.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Describa sloppy quorum y hinted handoff. ¿Qué ganan y qué complican?
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique qué rol juega anti-entropy en Dynamo.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique qué problema intenta resolver Spanner que no intenta resolver Dynamo.
\end{uaexercise}
\begin{uaanswer}
Spanner intenta resolver el problema de ofrecer consistencia fuerte global y transacciones distribuidas correctas a escala planetaria. Dynamo, en cambio, no busca imponer orden fuerte global; acepta reconciliación posterior para preservar disponibilidad y latencia.
\end{uaanswer}

\begin{uaexercise}
Explique qué es TrueTime y por qué devuelve un intervalo en lugar de un instante exacto.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Describa la idea de commit wait y explique su costo.
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique qué significa external consistency y por qué es más fuerte que eventual consistency.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en CAP, PACELC, quórums, Dynamo, vector clocks, Spanner, TrueTime y consistencia externa. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo partición inter-región, réplica atrasada, conflicto concurrente e incertidumbre temporal. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Explique por qué Spanner sacrifica latencia incluso en ausencia de partición.
\end{uaexercise}
\begin{uaanswer}
La respuesta debe situarse en CAP, PACELC, quórums, Dynamo, vector clocks, Spanner, TrueTime y consistencia externa. Se comienza declarando el supuesto decisivo y el comportamiento observable; luego se recorre una ejecución nominal y otra bajo partición inter-región, réplica atrasada, conflicto concurrente e incertidumbre temporal. El mecanismo elegido debe vincularse con una garantía concreta, evitando afirmar propiedades fuera de su alcance.

El argumento debe identificar la hipótesis, construir la cadena lógica o el contraejemplo y concluir exactamente qué propiedad queda demostrada. Una intuición sin secuencia verificable no es suficiente.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Compare Dynamo y Spanner en términos de:
\begin{itemize}
\item modelo de consistencia
\item latencia
\item particiones
\item conflictos
\end{itemize}
\end{uaexercise}
\begin{uaanswer}
La idempotencia separa intentos físicos de una operación lógica. Se usa una clave estable, se persiste el resultado junto con el efecto y los reintentos devuelven ese mismo resultado. El costo es estado adicional, política de expiración y coordinación con el almacenamiento.

La comparación debe usar al menos semántica, latencia, disponibilidad, complejidad operativa y recuperación. La alternativa preferible cambia cuando cambia el costo del error en el dominio; no existe ganador universal.

La evidencia apropiada sería decisión CAP/PACELC por operación, versiones y latencia inter-región. También debe indicarse una condición que refute la elección o haga preferible una solución más simple.
\end{uaanswer}

\begin{uaexercise}
Indique para qué tipo de aplicación elegiría Dynamo y para cuál elegiría Spanner.
\end{uaexercise}
\begin{uaanswer}
Elegiría Dynamo para aplicaciones donde:
\begin{itemize}
\item pequeñas inconsistencias temporales son aceptables;
\item la disponibilidad continua es prioritaria;
\item la reconciliación posterior tiene sentido.
\end{itemize}

Elegiría Spanner para aplicaciones donde:
\begin{itemize}
\item el dominio exige transacciones correctas globalmente;
\item los errores de consistencia son extremadamente costosos;
\item la latencia extra es aceptable.
\end{itemize}
\end{uaanswer}

\end{document}
